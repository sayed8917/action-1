name: My first action

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: wellcome massage
          run: echo "My first github action job"

        - name: List files
          run: ls
        


